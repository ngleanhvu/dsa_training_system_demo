spring:
  application:
    name: dsa_training_system
  datasource:
    url: jdbc:mysql://localhost:3306/dsa_training_system_db_demo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    password: 1234
    username: root
  flyway:
    locations: classpath:db/migration
    enabled: true

aws:
  access_key_id: ${AWS_ACCESS_KEY_ID}
  secret_access_key: ${AWS_SECRET_ACCESS_KEY}
  region: ${AWS_REGION}
  s3_bucket_name: ${AWS_S3_BUCKET_NAME}

file:
  max_size: 5242880

judge0:
  api_key: d04506efa8msh718faea26492c69p16ea82jsnbbe88b36948d
  api_host: judge0-ce.p.rapidapi.com

server:
  port: 8000
